<ContentDialog
    x:Class="FluentFold.Views.FirstLaunchSetupDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Welcome to FluentFold"
    PrimaryButtonText="Confirm"
    CloseButtonText=""
    DefaultButton="Primary"
    Width="500"
    PrimaryButtonClick="OnPrimaryButtonClick">

    <StackPanel Spacing="20">
        <TextBlock
            Text="Choose your preferred experience"
            Style="{StaticResource SubtitleTextBlockStyle}"
            TextWrapping="Wrap" />

        <Grid ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Standard Mode Card -->
            <Border
                x:Name="StandardCard"
                Grid.Column="0"
                CornerRadius="12"
                Padding="16"
                BorderThickness="2"
                BorderBrush="{ThemeResource CardStrokeColorDefault}"
                Background="{ThemeResource CardBackgroundFillColorDefault}"
                Tapped="StandardCard_Tapped">
                <StackPanel Spacing="12" HorizontalAlignment="Center">
                    <TextBlock
                        Text="&#xE8B7;"
                        FontFamily="{ThemeResource SymbolThemeFontFamily}"
                        FontSize="40"
                        HorizontalAlignment="Center"
                        Foreground="{ThemeResource SystemAccentColor}" />
                    <TextBlock Text="Standard" FontWeight="SemiBold" HorizontalAlignment="Center" FontSize="18" />
                    <TextBlock
                        Text="Simple automated view with essential controls."
                        TextWrapping="Wrap"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        HorizontalAlignment="Center" />
                </StackPanel>
            </Border>

            <!-- Pro Mode Card -->
            <Border
                x:Name="ProCard"
                Grid.Column="1"
                CornerRadius="12"
                Padding="16"
                BorderThickness="2"
                BorderBrush="{ThemeResource CardStrokeColorDefault}"
                Background="{ThemeResource CardBackgroundFillColorDefault}"
                Tapped="ProCard_Tapped">
                <StackPanel Spacing="12" HorizontalAlignment="Center">
                    <TextBlock
                        Text="&#xE943;"
                        FontFamily="{ThemeResource SymbolThemeFontFamily}"
                        FontSize="40"
                        HorizontalAlignment="Center"
                        Foreground="{ThemeResource SystemAccentColor}" />
                    <TextBlock Text="Pro" FontWeight="SemiBold" HorizontalAlignment="Center" FontSize="18" />
                    <TextBlock
                        Text="Advanced view with custom rules, naming, and filters."
                        TextWrapping="Wrap"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        HorizontalAlignment="Center" />
                </StackPanel>
            </Border>
        </Grid>
    </StackPanel>
</ContentDialog>
